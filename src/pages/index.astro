---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---

<!doctype html>
<html lang="en">
<head>
  <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
  <style>
    body { font-family: 'Arial', sans-serif; margin: 0; padding: 0; background: #0e0e1a; color: #f0e6d2; }
    .hero {
      position: relative;
      height: 70vh;
      background: url('https://skycoach.gg/storage/uploads/blog-posts/431/images/431_1704458122_900x700.jpg') center/cover no-repeat;
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
    }
    .hero-overlay {
      background: rgba(0, 0, 0, 0.65);
      padding: 3rem;
      border-radius: 15px;
      max-width: 90%;
    }
    .hero h1 { font-size: 4.5rem; margin: 0; color: #ffd700; text-shadow: 0 0 15px #ffd700; }
    .hero p { font-size: 1.8rem; color: #fff; }
    .welcome-section {
      max-width: 900px;
      margin: 4rem auto;
      padding: 3rem;
      text-align: center;
      background: rgba(255, 215, 0, 0.1);
      border-radius: 20px;
      box-shadow: 0 0 20px rgba(255, 215, 0, 0.3);
    }
    .welcome-section h3 { color: #ffd700; font-size: 2.5rem; }
    .welcome-section p { font-size: 1.2rem; line-height: 1.8; }
    .gallery {
      max-width: 1400px;
      margin: 4rem auto;
      padding: 2rem;
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
      gap: 2.5rem;
    }
    .gallery h3 { grid-column: 1 / -1; text-align: center; color: #ffd700; font-size: 2.2rem; }
    .gallery img {
      width: 100%;
      height: auto;
      border-radius: 15px;
      box-shadow: 0 0 20px #ffd700;
      transition: transform 0.4s ease;
    }
    .gallery img:hover { transform: scale(1.08); }
    @media (max-width: 768px) {
      .hero h1 { font-size: 3rem; }
      .hero p { font-size: 1.4rem; }
      .welcome-section { padding: 2rem; }
    }
  </style>
</head>
	<body>
		<Header />
<section class="hero">
    <div class="hero-overlay">
      <h3>圣光忽悠</h3>
      <p>圣骑士的乐子人聚集地</p>
    </div>
  </section>
		<main>
<div class="welcome-section">
  <h3>欢迎来到【圣光忽悠】</h3>
    <p>在艾泽拉斯的魔潮之下，圣骑士不是一本正经的布道者，而是高举锤子、带着圣光到处找乐子的乐子人。</p>
    <p>我们把所有严肃的机制拆开、把所有天赋玩出花样、把所有副本变成我们的游乐场。</p>
    <p>圣光忽悠着你，我们也忽悠着圣光。</p>
</div>
		</main>
		<Footer />
	</body>
</html>
